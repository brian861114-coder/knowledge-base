# Frontend Customization

The prototype already works for this starter vault. The first useful customizations are:

- tune domain descriptions and colors in `prototype/app.js`
- tune type labels if you rename note types
- add stronger copy in `prototype/index.html` if this becomes a public site

Do not patch exported JSON by hand. Change source notes, then re-export.
